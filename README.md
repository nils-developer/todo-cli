# todo-cli

This is a todo command-line application built in Rust. <br>
The focus in this small project is to become familiar with Rust.

### Features
- Add **one** task
```
cargo run add <task>
```

- List **all** tasks
```
cargo run list
```

- Remove **one** task
```
cargo run remove <index>
```
